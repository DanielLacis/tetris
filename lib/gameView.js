(function () {
  if (typeof Tetris === 'undefined') {
    Tetris = {};
  }
  var GameView = Tetris.GameView = function (game, canvas) {
    this.game = game;
    this.canvas = canvas;
  };

  GameView.prototype.start = function() {

  };


}());
